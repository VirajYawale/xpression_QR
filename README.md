# xpression_QR

User scans QR ->  QR opens URL with unique token -> Backend checks token status -> {If unused → allow + mark as used
If used → deny access}

mongoURL:
mongodb+srv://yawalevm_db_user:85vZ06zc4PIbMHNH@cluster0.df7cmim.mongodb.net/?appName=Cluster0